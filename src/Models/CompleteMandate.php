<?php

declare(strict_types=1);

/*
 * VisaAcceptanceMergedSpecLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace VisaAcceptanceMergedSpecLib\Models;

use stdClass;
use VisaAcceptanceMergedSpecLib\ApiHelper;

/**
 * The completeMandate object is designed to provide instructions for orchestrating payment services.
 * Unified Checkout is capable of orchestrating a number of services on your behalf.<br><br>
 * By providing this field in the capture context Unified Checkout will initiate services on your
 * behalf from the browser, simplifying your integration.
 */
class CompleteMandate implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $type;

    /**
     * @var bool|null
     */
    private $decisionManager;

    /**
     * @var bool|null
     */
    private $consumerAuthentication;

    /**
     * Returns Type.
     * This field is used to indicate how a payment should be processed.
     *
     * Possible values:
     * - AUTH: Use this value when you want to authorize a payment within Unified Checkout without
     * capturing it immediately.  Payment types that initiate an immediate transfer of funds are NOT
     * allowed.  If a capture context request includes a payment type incompatible with this mode, a 400
     * error will be returned.  A merchant would need to perform their own capture via API where applicable.
     * <br><br>
     * - CAPTURE: Use this value when you want to perform a sale within Unified Checkout and capture the
     * payment immediately during the transaction.  Note: Some payment types may return a PENDING status,
     * requiring an additional status check call to determine the final outcome of the payment.<br><br>
     * - PREFER_AUTH: Use this value to offer multiple alternative payment options during the Unified
     * Checkout experience. This option authorizes the payment without immediate capture, where available.
     * It will perform a "CAPTURE" where an "AUTH" is not allowed by the payment type.  Transactions can be
     * AUTHORIZED, CAPTURED, or PENDING.  If an "AUTH" is performed, a merchant would need to perform their
     * own capture via API where applicable.
     */
    public function getType(): ?string
    {
        return $this->type;
    }

    /**
     * Sets Type.
     * This field is used to indicate how a payment should be processed.
     *
     * Possible values:
     * - AUTH: Use this value when you want to authorize a payment within Unified Checkout without
     * capturing it immediately.  Payment types that initiate an immediate transfer of funds are NOT
     * allowed.  If a capture context request includes a payment type incompatible with this mode, a 400
     * error will be returned.  A merchant would need to perform their own capture via API where applicable.
     * <br><br>
     * - CAPTURE: Use this value when you want to perform a sale within Unified Checkout and capture the
     * payment immediately during the transaction.  Note: Some payment types may return a PENDING status,
     * requiring an additional status check call to determine the final outcome of the payment.<br><br>
     * - PREFER_AUTH: Use this value to offer multiple alternative payment options during the Unified
     * Checkout experience. This option authorizes the payment without immediate capture, where available.
     * It will perform a "CAPTURE" where an "AUTH" is not allowed by the payment type.  Transactions can be
     * AUTHORIZED, CAPTURED, or PENDING.  If an "AUTH" is performed, a merchant would need to perform their
     * own capture via API where applicable.
     *
     * @maps type
     */
    public function setType(?string $type): void
    {
        $this->type = $type;
    }

    /**
     * Returns Decision Manager.
     * Configure Unified Checkout to determine whether Decision Manager is invoked during service
     * orchestration.
     *
     * Possible values:
     * - True
     * - False<br><br>
     *
     * Setting this value to True indicates that device fingerprinting will be executed to add additional
     * information for risk service
     * Setting this value to False (or not provided) indicates that you do not wish to run device
     * fingerprinting and skip decision manager services.
     */
    public function getDecisionManager(): ?bool
    {
        return $this->decisionManager;
    }

    /**
     * Sets Decision Manager.
     * Configure Unified Checkout to determine whether Decision Manager is invoked during service
     * orchestration.
     *
     * Possible values:
     * - True
     * - False<br><br>
     *
     * Setting this value to True indicates that device fingerprinting will be executed to add additional
     * information for risk service
     * Setting this value to False (or not provided) indicates that you do not wish to run device
     * fingerprinting and skip decision manager services.
     *
     * @maps decisionManager
     */
    public function setDecisionManager(?bool $decisionManager): void
    {
        $this->decisionManager = $decisionManager;
    }

    /**
     * Returns Consumer Authentication.
     * Configure Unified Checkout to determine whether Consumer Authentication is invoked during service
     * orchestration.
     *
     * Possible values:
     * - True
     * - False<br><br>
     *
     * Setting this value to True will attempt to perform authentication using the Payer Authentication
     * Service.
     * Setting this value to False (or not provided) indicates that you do not wish to perform
     * authentication using the Payer Authentication Service.
     */
    public function getConsumerAuthentication(): ?bool
    {
        return $this->consumerAuthentication;
    }

    /**
     * Sets Consumer Authentication.
     * Configure Unified Checkout to determine whether Consumer Authentication is invoked during service
     * orchestration.
     *
     * Possible values:
     * - True
     * - False<br><br>
     *
     * Setting this value to True will attempt to perform authentication using the Payer Authentication
     * Service.
     * Setting this value to False (or not provided) indicates that you do not wish to perform
     * authentication using the Payer Authentication Service.
     *
     * @maps consumerAuthentication
     */
    public function setConsumerAuthentication(?bool $consumerAuthentication): void
    {
        $this->consumerAuthentication = $consumerAuthentication;
    }

    /**
     * Converts the CompleteMandate object to a human-readable string representation.
     *
     * @return string The string representation of the CompleteMandate object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'CompleteMandate',
            [
                'type' => $this->type,
                'decisionManager' => $this->decisionManager,
                'consumerAuthentication' => $this->consumerAuthentication,
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = ['type', 'decisionManager', 'consumerAuthentication'];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->type)) {
            $json['type']                   = $this->type;
        }
        if (isset($this->decisionManager)) {
            $json['decisionManager']        = $this->decisionManager;
        }
        if (isset($this->consumerAuthentication)) {
            $json['consumerAuthentication'] = $this->consumerAuthentication;
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
