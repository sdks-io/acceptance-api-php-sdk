<?php

declare(strict_types=1);

/*
 * VisaAcceptanceMergedSpecLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace VisaAcceptanceMergedSpecLib\Models;

use stdClass;
use VisaAcceptanceMergedSpecLib\ApiHelper;

class Card1 implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $suffix;

    /**
     * @var string|null
     */
    private $expirationMonth;

    /**
     * @var string|null
     */
    private $expirationYear;

    /**
     * @var string|null
     */
    private $type;

    /**
     * @var string|null
     */
    private $prefix;

    /**
     * @var string|null
     */
    private $hashedNumber;

    /**
     * Returns Suffix.
     * Last four digits of the cardholder’s account number. This field is included in the reply message
     * when the client software
     * that is installed on the POS terminal uses the token management service (TMS) to retrieve tokenized
     * payment details.
     *
     * You must contact customer support to have your account enabled to receive these fields in the credit
     * reply message.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * #### PIN debit
     * This field is returned only for tokenized transactions. You can use this value on the receipt that
     * you give to the cardholder.
     *
     * Returned by PIN debit credit and PIN debit purchase.
     *
     * This field is supported only by the following processors:
     * - American Express Direct
     * - Credit Mutuel-CIC
     * - FDC Nashville Global
     * - OmniPay Direct
     * - SIX
     */
    public function getSuffix(): ?string
    {
        return $this->suffix;
    }

    /**
     * Sets Suffix.
     * Last four digits of the cardholder’s account number. This field is included in the reply message
     * when the client software
     * that is installed on the POS terminal uses the token management service (TMS) to retrieve tokenized
     * payment details.
     *
     * You must contact customer support to have your account enabled to receive these fields in the credit
     * reply message.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * #### PIN debit
     * This field is returned only for tokenized transactions. You can use this value on the receipt that
     * you give to the cardholder.
     *
     * Returned by PIN debit credit and PIN debit purchase.
     *
     * This field is supported only by the following processors:
     * - American Express Direct
     * - Credit Mutuel-CIC
     * - FDC Nashville Global
     * - OmniPay Direct
     * - SIX
     *
     * @maps suffix
     */
    public function setSuffix(?string $suffix): void
    {
        $this->suffix = $suffix;
    }

    /**
     * Returns Expiration Month.
     * Two-digit month in which the payment card expires.
     *
     * Format: `MM`.
     *
     * Valid values: `01` through `12`. Leading 0 is required.
     *
     * #### Barclays and Streamline
     * For Maestro (UK Domestic) and Maestro (International) cards on Barclays and Streamline, this must be
     * a valid value
     * (`01` through `12`) but is not required to be a valid expiration date. In other words, an expiration
     * date that is
     * in the past does not cause Visa Acceptance to reject your request. However, an invalid expiration
     * date might cause
     * the issuer to reject your request.
     *
     * #### Encoded Account Numbers
     * For encoded account numbers (_type_=039), if there is no expiration date on the card, use `12`.
     *
     * #### FDMS Nashville
     * Required field.
     *
     * #### All other processors
     * Required if `pointOfSaleInformation.entryMode=keyed`. However, this field is optional if your
     * account is configured
     * for relaxed requirements for address data and expiration date. **Important** It is your
     * responsibility to determine
     * whether a field is required for the transaction you are requesting.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     */
    public function getExpirationMonth(): ?string
    {
        return $this->expirationMonth;
    }

    /**
     * Sets Expiration Month.
     * Two-digit month in which the payment card expires.
     *
     * Format: `MM`.
     *
     * Valid values: `01` through `12`. Leading 0 is required.
     *
     * #### Barclays and Streamline
     * For Maestro (UK Domestic) and Maestro (International) cards on Barclays and Streamline, this must be
     * a valid value
     * (`01` through `12`) but is not required to be a valid expiration date. In other words, an expiration
     * date that is
     * in the past does not cause Visa Acceptance to reject your request. However, an invalid expiration
     * date might cause
     * the issuer to reject your request.
     *
     * #### Encoded Account Numbers
     * For encoded account numbers (_type_=039), if there is no expiration date on the card, use `12`.
     *
     * #### FDMS Nashville
     * Required field.
     *
     * #### All other processors
     * Required if `pointOfSaleInformation.entryMode=keyed`. However, this field is optional if your
     * account is configured
     * for relaxed requirements for address data and expiration date. **Important** It is your
     * responsibility to determine
     * whether a field is required for the transaction you are requesting.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * @maps expirationMonth
     */
    public function setExpirationMonth(?string $expirationMonth): void
    {
        $this->expirationMonth = $expirationMonth;
    }

    /**
     * Returns Expiration Year.
     * Four-digit year in which the payment card expires.
     *
     * Format: `YYYY`.
     *
     * #### Barclays and Streamline
     * For Maestro (UK Domestic) and Maestro (International) cards on Barclays and Streamline, this must be
     * a valid value (`1900` through `3000`) but is not required to be a valid expiration date. In other
     * words, an expiration date that is in the past does not cause Visa Acceptance to reject your request.
     * However, an invalid expiration date might cause the issuer to reject your request.
     *
     * #### Encoded Account Numbers
     * For encoded account numbers (**_type_**`=039`), if there is no expiration date on the card, use
     * `2021`.
     *
     * #### FDMS Nashville
     * Required field.
     *
     * #### FDC Nashville Global and FDMS South
     * You can send in 2 digits or 4 digits. If you send in 2 digits, they must be the last 2 digits of the
     * year.
     *
     * #### All other processors
     * Required if `pointOfSaleInformation.entryMode=keyed`. However, this field is optional if your
     * account is configured
     * for relaxed requirements for address data and expiration date. **Important** It is your
     * responsibility to determine
     * whether a field is required for the transaction you are requesting.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     */
    public function getExpirationYear(): ?string
    {
        return $this->expirationYear;
    }

    /**
     * Sets Expiration Year.
     * Four-digit year in which the payment card expires.
     *
     * Format: `YYYY`.
     *
     * #### Barclays and Streamline
     * For Maestro (UK Domestic) and Maestro (International) cards on Barclays and Streamline, this must be
     * a valid value (`1900` through `3000`) but is not required to be a valid expiration date. In other
     * words, an expiration date that is in the past does not cause Visa Acceptance to reject your request.
     * However, an invalid expiration date might cause the issuer to reject your request.
     *
     * #### Encoded Account Numbers
     * For encoded account numbers (**_type_**`=039`), if there is no expiration date on the card, use
     * `2021`.
     *
     * #### FDMS Nashville
     * Required field.
     *
     * #### FDC Nashville Global and FDMS South
     * You can send in 2 digits or 4 digits. If you send in 2 digits, they must be the last 2 digits of the
     * year.
     *
     * #### All other processors
     * Required if `pointOfSaleInformation.entryMode=keyed`. However, this field is optional if your
     * account is configured
     * for relaxed requirements for address data and expiration date. **Important** It is your
     * responsibility to determine
     * whether a field is required for the transaction you are requesting.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * @maps expirationYear
     */
    public function setExpirationYear(?string $expirationYear): void
    {
        $this->expirationYear = $expirationYear;
    }

    /**
     * Returns Type.
     * Three-digit value that indicates the card type.
     *
     * **IMPORTANT** It is strongly recommended that you include the card type field in request messages
     * even if it is
     * optional for your processor and card type. Omitting the card type can cause the transaction to be
     * processed with the wrong card type.
     *
     * Possible values:
     * - `001`: Visa. Use card type value `001` for Visa Electron.
     * - `002`: Mastercard, Eurocard[^1], which is a European regional brand of Mastercard.
     * - `003`: American Express
     * - `004`: Discover
     * - `005`: Diners Club
     * - `006`: Carte Blanche[^1]
     * - `007`: JCB[^1]
     * - `008`: Optima
     * - `009`: GE Private Label
     * - `010`: Beneficial Private Label
     * - `011`: Twinpay Credit Card
     * - `012`: Twinpay Debit Card
     * - `013`: WalMart
     * - `014`: Enroute[^1]
     * - `015`: Lowe's Consumer
     * - `016`: Home Depot Consumer
     * - `017`: MBNA
     * - `018`: Dick's Sportswear
     * - `019`: Casual Corner
     * - `020`: Sears
     * - `021`: JAL[^1]
     * - `023`: Disney Card
     * - `024`: Maestro (UK Domestic)[^1]
     * - `025`: Sam's Club Consumer
     * - `026`: Sam's Club Business
     * - `027`: Nico's
     * - `028`: Paymentech Bill Me Later
     * - `029`: Bebe
     * - `030`: Restoration Hardware
     * - `031`: Delta Online
     * - `032`: Solo
     * - `033`: Visa Electron[^1]. Do not use this value. Use `001` for all Visa card types.
     * - `034`: Dankort[^1]
     * - `035`: Laser
     * - `036`: Cartes Bancaires[^1,4]
     * - `037`: Carta Si[^1]
     * - `038`: Pinless Debit
     * - `039`: Encoded account number[^1]
     * - `040`: UATP[^1]
     * - `041`: HOUSEHOLD
     * - `042`: Maestro (International)[^1]
     * - `043`: GE MONEY
     * - `044`: Korean Cards
     * - `045`: Style Cards
     * - `046`: JCrew
     * - `047`: Payeasecn eWallet
     * - `048`: Payeasecn Bank Transfer
     * - `049`: Meijer
     * - `050`: Hipercard[^2,3]
     * - `051`: Aura
     * - `052`: Redecard
     * - `053`: Orico card
     * - `054`: Elo[^3]
     * - `055`: Capitol One Private Label
     * - `056`: Carnet
     * - `057`: Costco Private Label
     * - `058`: Carnet
     * - `059`: ValueLink
     * - `060`: MADA
     * - `061`: RuPay
     * - `062`: China UnionPay
     * - `063`: Falabella Private Label
     * - `064`: Prompt Card
     * - `065`: Korean Domestic
     * - `066`: Banricompras
     * - `067`: MEEZA
     * - `068`: PayPak
     * - `070`: EFTPOS
     * - `071`: Codensa
     * - `072`: Olimpica
     * - `073`: Colsubsidio
     * - `074`: Tuya
     * - `075`: Sodexo
     * - `076`: Naranja
     * - `077`: Cabal
     * - `078`: DINELCO
     * - `079`: PANAL
     * - `080`: EPM
     * - `081`: Jaywan
     *
     * [^1]: For this card type, you must include the `paymentInformation.card.type` or `paymentInformation.
     * tokenizedCard.type` field in your request for an authorization or a stand-alone credit.
     * [^2]: For this card type on Cielo 3.0, you must include the `paymentInformation.card.type` or
     * `paymentInformation.tokenizedCard.type` field in a request for an authorization or a stand-alone
     * credit. This card type is not supported on Cielo 1.5.
     * [^3]: For this card type on Getnet and Rede, you must include the `paymentInformation.card.type` or
     * `paymentInformation.tokenizedCard.type` field in a request for an authorization or a stand-alone
     * credit.
     * [^4]: For this card type, you must include the `paymentInformation.card.type` in your request for
     * any payer authentication services.
     *
     * #### Used by
     * **Authorization**
     * Required for Carte Blanche and JCB.
     * Optional for all other card types.
     *
     * #### Card Present reply
     * This field is included in the reply message when the client software that is installed on the POS
     * terminal uses
     * the token management service (TMS) to retrieve tokenized payment details. You must contact customer
     * support to
     * have your account enabled to receive these fields in the credit reply message.
     *
     * Returned by the Credit service.
     *
     * This reply field is only supported by the following processors:
     * - American Express Direct
     * - Credit Mutuel-CIC
     * - FDC Nashville Global
     * - OmniPay Direct
     * - SIX
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * #### GPX
     * This field only supports transactions from the following card types:
     * - Visa
     * - Mastercard
     * - AMEX
     * - Discover
     * - Diners
     * - JCB
     * - Union Pay International
     */
    public function getType(): ?string
    {
        return $this->type;
    }

    /**
     * Sets Type.
     * Three-digit value that indicates the card type.
     *
     * **IMPORTANT** It is strongly recommended that you include the card type field in request messages
     * even if it is
     * optional for your processor and card type. Omitting the card type can cause the transaction to be
     * processed with the wrong card type.
     *
     * Possible values:
     * - `001`: Visa. Use card type value `001` for Visa Electron.
     * - `002`: Mastercard, Eurocard[^1], which is a European regional brand of Mastercard.
     * - `003`: American Express
     * - `004`: Discover
     * - `005`: Diners Club
     * - `006`: Carte Blanche[^1]
     * - `007`: JCB[^1]
     * - `008`: Optima
     * - `009`: GE Private Label
     * - `010`: Beneficial Private Label
     * - `011`: Twinpay Credit Card
     * - `012`: Twinpay Debit Card
     * - `013`: WalMart
     * - `014`: Enroute[^1]
     * - `015`: Lowe's Consumer
     * - `016`: Home Depot Consumer
     * - `017`: MBNA
     * - `018`: Dick's Sportswear
     * - `019`: Casual Corner
     * - `020`: Sears
     * - `021`: JAL[^1]
     * - `023`: Disney Card
     * - `024`: Maestro (UK Domestic)[^1]
     * - `025`: Sam's Club Consumer
     * - `026`: Sam's Club Business
     * - `027`: Nico's
     * - `028`: Paymentech Bill Me Later
     * - `029`: Bebe
     * - `030`: Restoration Hardware
     * - `031`: Delta Online
     * - `032`: Solo
     * - `033`: Visa Electron[^1]. Do not use this value. Use `001` for all Visa card types.
     * - `034`: Dankort[^1]
     * - `035`: Laser
     * - `036`: Cartes Bancaires[^1,4]
     * - `037`: Carta Si[^1]
     * - `038`: Pinless Debit
     * - `039`: Encoded account number[^1]
     * - `040`: UATP[^1]
     * - `041`: HOUSEHOLD
     * - `042`: Maestro (International)[^1]
     * - `043`: GE MONEY
     * - `044`: Korean Cards
     * - `045`: Style Cards
     * - `046`: JCrew
     * - `047`: Payeasecn eWallet
     * - `048`: Payeasecn Bank Transfer
     * - `049`: Meijer
     * - `050`: Hipercard[^2,3]
     * - `051`: Aura
     * - `052`: Redecard
     * - `053`: Orico card
     * - `054`: Elo[^3]
     * - `055`: Capitol One Private Label
     * - `056`: Carnet
     * - `057`: Costco Private Label
     * - `058`: Carnet
     * - `059`: ValueLink
     * - `060`: MADA
     * - `061`: RuPay
     * - `062`: China UnionPay
     * - `063`: Falabella Private Label
     * - `064`: Prompt Card
     * - `065`: Korean Domestic
     * - `066`: Banricompras
     * - `067`: MEEZA
     * - `068`: PayPak
     * - `070`: EFTPOS
     * - `071`: Codensa
     * - `072`: Olimpica
     * - `073`: Colsubsidio
     * - `074`: Tuya
     * - `075`: Sodexo
     * - `076`: Naranja
     * - `077`: Cabal
     * - `078`: DINELCO
     * - `079`: PANAL
     * - `080`: EPM
     * - `081`: Jaywan
     *
     * [^1]: For this card type, you must include the `paymentInformation.card.type` or `paymentInformation.
     * tokenizedCard.type` field in your request for an authorization or a stand-alone credit.
     * [^2]: For this card type on Cielo 3.0, you must include the `paymentInformation.card.type` or
     * `paymentInformation.tokenizedCard.type` field in a request for an authorization or a stand-alone
     * credit. This card type is not supported on Cielo 1.5.
     * [^3]: For this card type on Getnet and Rede, you must include the `paymentInformation.card.type` or
     * `paymentInformation.tokenizedCard.type` field in a request for an authorization or a stand-alone
     * credit.
     * [^4]: For this card type, you must include the `paymentInformation.card.type` in your request for
     * any payer authentication services.
     *
     * #### Used by
     * **Authorization**
     * Required for Carte Blanche and JCB.
     * Optional for all other card types.
     *
     * #### Card Present reply
     * This field is included in the reply message when the client software that is installed on the POS
     * terminal uses
     * the token management service (TMS) to retrieve tokenized payment details. You must contact customer
     * support to
     * have your account enabled to receive these fields in the credit reply message.
     *
     * Returned by the Credit service.
     *
     * This reply field is only supported by the following processors:
     * - American Express Direct
     * - Credit Mutuel-CIC
     * - FDC Nashville Global
     * - OmniPay Direct
     * - SIX
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * #### GPX
     * This field only supports transactions from the following card types:
     * - Visa
     * - Mastercard
     * - AMEX
     * - Discover
     * - Diners
     * - JCB
     * - Union Pay International
     *
     * @maps type
     */
    public function setType(?string $type): void
    {
        $this->type = $type;
    }

    /**
     * Returns Prefix.
     * Bank Identification Number (BIN). This is the initial four to six numbers on a credit card account
     * number.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     */
    public function getPrefix(): ?string
    {
        return $this->prefix;
    }

    /**
     * Sets Prefix.
     * Bank Identification Number (BIN). This is the initial four to six numbers on a credit card account
     * number.
     *
     * #### Google Pay transactions
     * For PAN-based Google Pay transactions, this field is returned in the API response.
     *
     * @maps prefix
     */
    public function setPrefix(?string $prefix): void
    {
        $this->prefix = $prefix;
    }

    /**
     * Returns Hashed Number.
     * #### Visa Platform Connect
     * This API field will contain the SHA 256 hashed value of PAN.
     */
    public function getHashedNumber(): ?string
    {
        return $this->hashedNumber;
    }

    /**
     * Sets Hashed Number.
     * #### Visa Platform Connect
     * This API field will contain the SHA 256 hashed value of PAN.
     *
     * @maps hashedNumber
     */
    public function setHashedNumber(?string $hashedNumber): void
    {
        $this->hashedNumber = $hashedNumber;
    }

    /**
     * Converts the Card1 object to a human-readable string representation.
     *
     * @return string The string representation of the Card1 object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'Card1',
            [
                'suffix' => $this->suffix,
                'expirationMonth' => $this->expirationMonth,
                'expirationYear' => $this->expirationYear,
                'type' => $this->type,
                'prefix' => $this->prefix,
                'hashedNumber' => $this->hashedNumber,
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = ['suffix', 'expirationMonth', 'expirationYear', 'type', 'prefix', 'hashedNumber'];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->suffix)) {
            $json['suffix']          = $this->suffix;
        }
        if (isset($this->expirationMonth)) {
            $json['expirationMonth'] = $this->expirationMonth;
        }
        if (isset($this->expirationYear)) {
            $json['expirationYear']  = $this->expirationYear;
        }
        if (isset($this->type)) {
            $json['type']            = $this->type;
        }
        if (isset($this->prefix)) {
            $json['prefix']          = $this->prefix;
        }
        if (isset($this->hashedNumber)) {
            $json['hashedNumber']    = $this->hashedNumber;
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
