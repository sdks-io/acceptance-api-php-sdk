<?php

declare(strict_types=1);

/*
 * VisaAcceptanceMergedSpecLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace VisaAcceptanceMergedSpecLib\Models;

use stdClass;
use VisaAcceptanceMergedSpecLib\ApiHelper;

/**
 * Contains detailed response information about the customer's IP address.
 */
class IpAddress implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $anonymizerStatus;

    /**
     * @var string|null
     */
    private $locality;

    /**
     * @var string|null
     */
    private $country;

    /**
     * @var string|null
     */
    private $administrativeArea;

    /**
     * @var string|null
     */
    private $routingMethod;

    /**
     * @var string|null
     */
    private $carrier;

    /**
     * @var string|null
     */
    private $organization;

    /**
     * Returns Anonymizer Status.
     * Indicates whether the transaction IP address is associated with a known anonymous proxy.
     *
     * For all possible values, see the `score_ip_anonymizer_status` field description in the _Decision
     * Manager Using the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click
     * **Decision Manager** > **Documentation** > **Guides** > _Decision Manager Using the SCMP API
     * Developer Guide_ (PDF link).
     */
    public function getAnonymizerStatus(): ?string
    {
        return $this->anonymizerStatus;
    }

    /**
     * Sets Anonymizer Status.
     * Indicates whether the transaction IP address is associated with a known anonymous proxy.
     *
     * For all possible values, see the `score_ip_anonymizer_status` field description in the _Decision
     * Manager Using the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click
     * **Decision Manager** > **Documentation** > **Guides** > _Decision Manager Using the SCMP API
     * Developer Guide_ (PDF link).
     *
     * @maps anonymizerStatus
     */
    public function setAnonymizerStatus(?string $anonymizerStatus): void
    {
        $this->anonymizerStatus = $anonymizerStatus;
    }

    /**
     * Returns Locality.
     * Name of the city decoded from the IP address used directly or indirectly by the customer to send the
     * order.
     *
     * For all possible values, see the `score_ip_city` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     */
    public function getLocality(): ?string
    {
        return $this->locality;
    }

    /**
     * Sets Locality.
     * Name of the city decoded from the IP address used directly or indirectly by the customer to send the
     * order.
     *
     * For all possible values, see the `score_ip_city` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     *
     * @maps locality
     */
    public function setLocality(?string $locality): void
    {
        $this->locality = $locality;
    }

    /**
     * Returns Country.
     * Name of the country decoded from the IP address used directly or indirectly by the customer to send
     * the order.
     *
     * For all possible values, see the `score_ip_country` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     */
    public function getCountry(): ?string
    {
        return $this->country;
    }

    /**
     * Sets Country.
     * Name of the country decoded from the IP address used directly or indirectly by the customer to send
     * the order.
     *
     * For all possible values, see the `score_ip_country` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     *
     * @maps country
     */
    public function setCountry(?string $country): void
    {
        $this->country = $country;
    }

    /**
     * Returns Administrative Area.
     * Name of the state decoded from the IP address used directly or indirectly by the customer to send
     * the order.
     *
     * For all possible values, see the `score_ip_state` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     */
    public function getAdministrativeArea(): ?string
    {
        return $this->administrativeArea;
    }

    /**
     * Sets Administrative Area.
     * Name of the state decoded from the IP address used directly or indirectly by the customer to send
     * the order.
     *
     * For all possible values, see the `score_ip_state` field description in the _Decision Manager Using
     * the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click **Decision Manager**
     * > **Documentation** > **Guides** > _Decision Manager Using the SCMP API Developer Guide_ (PDF link).
     *
     * @maps administrativeArea
     */
    public function setAdministrativeArea(?string $administrativeArea): void
    {
        $this->administrativeArea = $administrativeArea;
    }

    /**
     * Returns Routing Method.
     * Routing method decoded from the IP address used directly or indirectly by the customer to send the
     * order.
     *
     * For all possible values, see the `score_ip_routing_method` field description in the _Decision
     * Manager Using the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click
     * **Decision Manager** > **Documentation** > **Guides** > _Decision Manager Using the SCMP API
     * Developer Guide_ (PDF link).
     */
    public function getRoutingMethod(): ?string
    {
        return $this->routingMethod;
    }

    /**
     * Sets Routing Method.
     * Routing method decoded from the IP address used directly or indirectly by the customer to send the
     * order.
     *
     * For all possible values, see the `score_ip_routing_method` field description in the _Decision
     * Manager Using the SCMP API Developer Guide_ on the [Visa Acceptance Business Center.]( Click
     * **Decision Manager** > **Documentation** > **Guides** > _Decision Manager Using the SCMP API
     * Developer Guide_ (PDF link).
     *
     * @maps routingMethod
     */
    public function setRoutingMethod(?string $routingMethod): void
    {
        $this->routingMethod = $routingMethod;
    }

    /**
     * Returns Carrier.
     * Provides the name of the organization that owns the ASN. The carrier is responsible for the traffic
     * carried on the network or set of networks designated as an Autonomous System (AS) and identified by
     * the ASN.
     * While there are more than 27,000 active ASNs, there are fewer carriers, because a single carrier
     * often manages several ASNs.
     */
    public function getCarrier(): ?string
    {
        return $this->carrier;
    }

    /**
     * Sets Carrier.
     * Provides the name of the organization that owns the ASN. The carrier is responsible for the traffic
     * carried on the network or set of networks designated as an Autonomous System (AS) and identified by
     * the ASN.
     * While there are more than 27,000 active ASNs, there are fewer carriers, because a single carrier
     * often manages several ASNs.
     *
     * @maps carrier
     */
    public function setCarrier(?string $carrier): void
    {
        $this->carrier = $carrier;
    }

    /**
     * Returns Organization.
     * The Registering Organization is the entity responsible for the actions and content associated with a
     * given block of IP addresses. This is in contrast to the carrier, which is responsible for the
     * routing of traffic for network blocks. Registering Organizations include many types of entities,
     * including corporate, government, or educational entities, and ISPs managing the allocation and use
     * of network blocks.
     */
    public function getOrganization(): ?string
    {
        return $this->organization;
    }

    /**
     * Sets Organization.
     * The Registering Organization is the entity responsible for the actions and content associated with a
     * given block of IP addresses. This is in contrast to the carrier, which is responsible for the
     * routing of traffic for network blocks. Registering Organizations include many types of entities,
     * including corporate, government, or educational entities, and ISPs managing the allocation and use
     * of network blocks.
     *
     * @maps organization
     */
    public function setOrganization(?string $organization): void
    {
        $this->organization = $organization;
    }

    /**
     * Converts the IpAddress object to a human-readable string representation.
     *
     * @return string The string representation of the IpAddress object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'IpAddress',
            [
                'anonymizerStatus' => $this->anonymizerStatus,
                'locality' => $this->locality,
                'country' => $this->country,
                'administrativeArea' => $this->administrativeArea,
                'routingMethod' => $this->routingMethod,
                'carrier' => $this->carrier,
                'organization' => $this->organization,
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = [
        'anonymizerStatus',
        'locality',
        'country',
        'administrativeArea',
        'routingMethod',
        'carrier',
        'organization'
    ];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->anonymizerStatus)) {
            $json['anonymizerStatus']   = $this->anonymizerStatus;
        }
        if (isset($this->locality)) {
            $json['locality']           = $this->locality;
        }
        if (isset($this->country)) {
            $json['country']            = $this->country;
        }
        if (isset($this->administrativeArea)) {
            $json['administrativeArea'] = $this->administrativeArea;
        }
        if (isset($this->routingMethod)) {
            $json['routingMethod']      = $this->routingMethod;
        }
        if (isset($this->carrier)) {
            $json['carrier']            = $this->carrier;
        }
        if (isset($this->organization)) {
            $json['organization']       = $this->organization;
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
