<?php

declare(strict_types=1);

/*
 * VisaAcceptanceMergedSpecLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace VisaAcceptanceMergedSpecLib\Models;

use stdClass;
use VisaAcceptanceMergedSpecLib\ApiHelper;

class CaptureOptions implements \JsonSerializable
{
    /**
     * @var int|null
     */
    private $captureSequenceNumber;

    /**
     * @var int|null
     */
    private $totalCaptureCount;

    /**
     * @var string|null
     */
    private $dateToCapture;

    /**
     * @var string|null
     */
    private $isFinal;

    /**
     * @var string|null
     */
    private $notes;

    /**
     * @var string|null
     */
    private $reconciliationId;

    /**
     * @var string|null
     */
    private $reconciliationIdAlternate;

    /**
     * Returns Capture Sequence Number.
     * Capture number when requesting multiple partial captures for one authorization.
     * Used along with `totalCaptureCount` to track which capture is being processed.
     *
     * For example, the second of five captures would be passed to Visa Acceptance as:
     * - `captureSequenceNumber_ = 2`, and
     * - `totalCaptureCount = 5`
     */
    public function getCaptureSequenceNumber(): ?int
    {
        return $this->captureSequenceNumber;
    }

    /**
     * Sets Capture Sequence Number.
     * Capture number when requesting multiple partial captures for one authorization.
     * Used along with `totalCaptureCount` to track which capture is being processed.
     *
     * For example, the second of five captures would be passed to Visa Acceptance as:
     * - `captureSequenceNumber_ = 2`, and
     * - `totalCaptureCount = 5`
     *
     * @maps captureSequenceNumber
     */
    public function setCaptureSequenceNumber(?int $captureSequenceNumber): void
    {
        $this->captureSequenceNumber = $captureSequenceNumber;
    }

    /**
     * Returns Total Capture Count.
     * Total number of captures when requesting multiple partial captures for one payment.
     * Used along with `captureSequenceNumber` field to track which capture is being processed.
     *
     * For example, the second of five captures would be passed to Visa Acceptance as:
     * - `captureSequenceNumber = 2`, and
     * - `totalCaptureCount = 5`
     */
    public function getTotalCaptureCount(): ?int
    {
        return $this->totalCaptureCount;
    }

    /**
     * Sets Total Capture Count.
     * Total number of captures when requesting multiple partial captures for one payment.
     * Used along with `captureSequenceNumber` field to track which capture is being processed.
     *
     * For example, the second of five captures would be passed to Visa Acceptance as:
     * - `captureSequenceNumber = 2`, and
     * - `totalCaptureCount = 5`
     *
     * @maps totalCaptureCount
     */
    public function setTotalCaptureCount(?int $totalCaptureCount): void
    {
        $this->totalCaptureCount = $totalCaptureCount;
    }

    /**
     * Returns Date to Capture.
     * Date on which you want the capture to occur. This field is supported only for Visa Acceptance
     * through VisaNet.
     * Format: `MMDD`
     *
     * #### Used by
     * **Authorization**
     * Optional field.
     */
    public function getDateToCapture(): ?string
    {
        return $this->dateToCapture;
    }

    /**
     * Sets Date to Capture.
     * Date on which you want the capture to occur. This field is supported only for Visa Acceptance
     * through VisaNet.
     * Format: `MMDD`
     *
     * #### Used by
     * **Authorization**
     * Optional field.
     *
     * @maps dateToCapture
     */
    public function setDateToCapture(?string $dateToCapture): void
    {
        $this->dateToCapture = $dateToCapture;
    }

    /**
     * Returns Is Final.
     * Indicates whether to release the authorization hold on the remaining funds.
     * Possible Values:
     * - `true`
     * - `false`
     */
    public function getIsFinal(): ?string
    {
        return $this->isFinal;
    }

    /**
     * Sets Is Final.
     * Indicates whether to release the authorization hold on the remaining funds.
     * Possible Values:
     * - `true`
     * - `false`
     *
     * @maps isFinal
     */
    public function setIsFinal(?string $isFinal): void
    {
        $this->isFinal = $isFinal;
    }

    /**
     * Returns Notes.
     * An informational note about this settlement. Appears in both the payer's transaction history and the
     * emails that the payer receives.
     */
    public function getNotes(): ?string
    {
        return $this->notes;
    }

    /**
     * Sets Notes.
     * An informational note about this settlement. Appears in both the payer's transaction history and the
     * emails that the payer receives.
     *
     * @maps notes
     */
    public function setNotes(?string $notes): void
    {
        $this->notes = $notes;
    }

    /**
     * Returns Reconciliation Id.
     * Used for authbill request when capture field equals true
     */
    public function getReconciliationId(): ?string
    {
        return $this->reconciliationId;
    }

    /**
     * Sets Reconciliation Id.
     * Used for authbill request when capture field equals true
     *
     * @maps reconciliationId
     */
    public function setReconciliationId(?string $reconciliationId): void
    {
        $this->reconciliationId = $reconciliationId;
    }

    /**
     * Returns Reconciliation Id Alternate.
     * Used by Nike merchant to send 12 digit order number
     */
    public function getReconciliationIdAlternate(): ?string
    {
        return $this->reconciliationIdAlternate;
    }

    /**
     * Sets Reconciliation Id Alternate.
     * Used by Nike merchant to send 12 digit order number
     *
     * @maps reconciliationIdAlternate
     */
    public function setReconciliationIdAlternate(?string $reconciliationIdAlternate): void
    {
        $this->reconciliationIdAlternate = $reconciliationIdAlternate;
    }

    /**
     * Converts the CaptureOptions object to a human-readable string representation.
     *
     * @return string The string representation of the CaptureOptions object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'CaptureOptions',
            [
                'captureSequenceNumber' => $this->captureSequenceNumber,
                'totalCaptureCount' => $this->totalCaptureCount,
                'dateToCapture' => $this->dateToCapture,
                'isFinal' => $this->isFinal,
                'notes' => $this->notes,
                'reconciliationId' => $this->reconciliationId,
                'reconciliationIdAlternate' => $this->reconciliationIdAlternate,
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = [
        'captureSequenceNumber',
        'totalCaptureCount',
        'dateToCapture',
        'isFinal',
        'notes',
        'reconciliationId',
        'reconciliationIdAlternate'
    ];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->captureSequenceNumber)) {
            $json['captureSequenceNumber']     = $this->captureSequenceNumber;
        }
        if (isset($this->totalCaptureCount)) {
            $json['totalCaptureCount']         = $this->totalCaptureCount;
        }
        if (isset($this->dateToCapture)) {
            $json['dateToCapture']             = $this->dateToCapture;
        }
        if (isset($this->isFinal)) {
            $json['isFinal']                   = $this->isFinal;
        }
        if (isset($this->notes)) {
            $json['notes']                     = $this->notes;
        }
        if (isset($this->reconciliationId)) {
            $json['reconciliationId']          = $this->reconciliationId;
        }
        if (isset($this->reconciliationIdAlternate)) {
            $json['reconciliationIdAlternate'] = $this->reconciliationIdAlternate;
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
