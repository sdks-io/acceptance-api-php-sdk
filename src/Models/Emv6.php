<?php

declare(strict_types=1);

/*
 * VisaAcceptanceMergedSpecLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace VisaAcceptanceMergedSpecLib\Models;

use stdClass;
use VisaAcceptanceMergedSpecLib\ApiHelper;

class Emv6 implements \JsonSerializable
{
    /**
     * @var string|null
     */
    private $tags;

    /**
     * @var bool|null
     */
    private $fallback;

    /**
     * Returns Tags.
     * EMV data that is transmitted from the chip card to the issuer, and from the issuer to the chip card.
     * The EMV
     * data is in the tag-length-value format and includes chip card tags, terminal tags, and transaction
     * detail tags.
     *
     * For information about the individual tags, see the “Application Specification” section in the EMV 4.
     * 3 Specifications:
     *
     * **Note** Card present information about EMV applies only to credit card processing and PIN debit
     * processing.
     * All other card present information applies only to credit card processing. PIN debit processing is
     * available only
     * on FDC Nashville Global.
     *
     * **Important** The following tags contain sensitive information and **must not** be included in this
     * field:
     *
     * - `56`: Track 1 equivalent data
     * - `57`: Track 2 equivalent data
     * - `5A`: Application PAN
     * - `5F20`: Cardholder name
     * - `5F24`: Application expiration date (This sensitivity has been relaxed for Credit Mutuel-CIC,
     * American Express Direct, FDC Nashville Global, First Data Merchant Solutions, and SIX)
     * - `99`: Transaction PIN
     * - `9F0B`: Cardholder name (extended)
     * - `9F1F`: Track 1 discretionary data
     * - `9F20`: Track 2 discretionary data
     *
     * For captures, this field is required for contact EMV transactions. Otherwise, it is optional.
     *
     * For credits, this field is required for contact EMV stand-alone credits and contactless EMV stand-
     * alone credits.
     * Otherwise, it is optional.
     *
     * **Important** For contact EMV captures, contact EMV stand-alone credits, and contactless EMV stand-
     * alone credits,
     * you must include the following tags in this field. For all other types of EMV transactions, the
     * following tags
     * are optional.
     *
     * - `95`: Terminal verification results
     * - `9F10`: Issuer application data
     * - `9F26`: Application cryptogram
     *
     *
     * #### Visa Acceptance through VisaNet
     * - In Japan: 199 bytes
     * - In other countries: String (252)
     *
     * For Mastercard Transactions, Optionally Tag 9F60 (Authenticated Application Data) and
     * Tag 96 (Kernel Identifier - Terminal) can be included in the Field.
     *
     * #### GPX
     * This field only supports transactions from the following card types:
     * - Visa
     * - Mastercard
     * - AMEX
     * - Discover
     * - Diners
     * - JCB
     * - Union Pay International
     *
     * #### JCN Gateway
     * The following tags must be included:
     * - `4F`: Application identifier
     * - `84`: Dedicated file name
     *
     * Data length: 199 bytes
     *
     * #### All other processors:
     * String (999)
     *
     * #### Used by
     * Authorization: Optional
     * Authorization Reversal: Optional
     * Credit: Optional
     * PIN Debit processing (purchase, credit and reversal): Optional
     */
    public function getTags(): ?string
    {
        return $this->tags;
    }

    /**
     * Sets Tags.
     * EMV data that is transmitted from the chip card to the issuer, and from the issuer to the chip card.
     * The EMV
     * data is in the tag-length-value format and includes chip card tags, terminal tags, and transaction
     * detail tags.
     *
     * For information about the individual tags, see the “Application Specification” section in the EMV 4.
     * 3 Specifications:
     *
     * **Note** Card present information about EMV applies only to credit card processing and PIN debit
     * processing.
     * All other card present information applies only to credit card processing. PIN debit processing is
     * available only
     * on FDC Nashville Global.
     *
     * **Important** The following tags contain sensitive information and **must not** be included in this
     * field:
     *
     * - `56`: Track 1 equivalent data
     * - `57`: Track 2 equivalent data
     * - `5A`: Application PAN
     * - `5F20`: Cardholder name
     * - `5F24`: Application expiration date (This sensitivity has been relaxed for Credit Mutuel-CIC,
     * American Express Direct, FDC Nashville Global, First Data Merchant Solutions, and SIX)
     * - `99`: Transaction PIN
     * - `9F0B`: Cardholder name (extended)
     * - `9F1F`: Track 1 discretionary data
     * - `9F20`: Track 2 discretionary data
     *
     * For captures, this field is required for contact EMV transactions. Otherwise, it is optional.
     *
     * For credits, this field is required for contact EMV stand-alone credits and contactless EMV stand-
     * alone credits.
     * Otherwise, it is optional.
     *
     * **Important** For contact EMV captures, contact EMV stand-alone credits, and contactless EMV stand-
     * alone credits,
     * you must include the following tags in this field. For all other types of EMV transactions, the
     * following tags
     * are optional.
     *
     * - `95`: Terminal verification results
     * - `9F10`: Issuer application data
     * - `9F26`: Application cryptogram
     *
     *
     * #### Visa Acceptance through VisaNet
     * - In Japan: 199 bytes
     * - In other countries: String (252)
     *
     * For Mastercard Transactions, Optionally Tag 9F60 (Authenticated Application Data) and
     * Tag 96 (Kernel Identifier - Terminal) can be included in the Field.
     *
     * #### GPX
     * This field only supports transactions from the following card types:
     * - Visa
     * - Mastercard
     * - AMEX
     * - Discover
     * - Diners
     * - JCB
     * - Union Pay International
     *
     * #### JCN Gateway
     * The following tags must be included:
     * - `4F`: Application identifier
     * - `84`: Dedicated file name
     *
     * Data length: 199 bytes
     *
     * #### All other processors:
     * String (999)
     *
     * #### Used by
     * Authorization: Optional
     * Authorization Reversal: Optional
     * Credit: Optional
     * PIN Debit processing (purchase, credit and reversal): Optional
     *
     * @maps tags
     */
    public function setTags(?string $tags): void
    {
        $this->tags = $tags;
    }

    /**
     * Returns Fallback.
     * Indicates whether a fallback method was used to enter credit card information into the POS terminal.
     * When a
     * technical problem prevents a successful exchange of information between a chip card and a chip-
     * capable terminal:
     *
     * 1. Swipe the card or key the credit card information into the POS terminal.
     * 2. Use the pointOfSaleInformation.entryMode field to indicate whether the information was swiped or
     * keyed.
     *
     *
     * Possible values:
     * - `true`: Fallback method was used.
     * - `false` (default): Fallback method was not used.
     *
     * This field is supported only on American Express Direct, Chase Paymentech Solutions, Visa Acceptance
     * through VisaNet,
     * FDC Nashville Global, GPN, JCN Gateway, OmniPay Direct, and SIX.
     */
    public function getFallback(): ?bool
    {
        return $this->fallback;
    }

    /**
     * Sets Fallback.
     * Indicates whether a fallback method was used to enter credit card information into the POS terminal.
     * When a
     * technical problem prevents a successful exchange of information between a chip card and a chip-
     * capable terminal:
     *
     * 1. Swipe the card or key the credit card information into the POS terminal.
     * 2. Use the pointOfSaleInformation.entryMode field to indicate whether the information was swiped or
     * keyed.
     *
     *
     * Possible values:
     * - `true`: Fallback method was used.
     * - `false` (default): Fallback method was not used.
     *
     * This field is supported only on American Express Direct, Chase Paymentech Solutions, Visa Acceptance
     * through VisaNet,
     * FDC Nashville Global, GPN, JCN Gateway, OmniPay Direct, and SIX.
     *
     * @maps fallback
     */
    public function setFallback(?bool $fallback): void
    {
        $this->fallback = $fallback;
    }

    /**
     * Converts the Emv6 object to a human-readable string representation.
     *
     * @return string The string representation of the Emv6 object.
     */
    public function __toString(): string
    {
        return ApiHelper::stringify(
            'Emv6',
            [
                'tags' => $this->tags,
                'fallback' => $this->fallback,
                'additionalProperties' => $this->additionalProperties
            ]
        );
    }

    protected $propertyNames = ['tags', 'fallback'];

    private $additionalProperties = [];

    /**
     * Add an additional property to this model.
     *
     * @param string $name Name of property.
     * @param mixed $value Value of property.
     */
    public function addAdditionalProperty(string $name, $value)
    {
        if (in_array($name, $this->propertyNames, true)) {
            throw new \InvalidArgumentException(
                "The additional property key, '$name' conflicts with one of the model's properties"
            );
        }

        $this->additionalProperties[$name] = $value;
    }

    /**
     * Find an additional property by name in this model or false if property does not exist.
     *
     * @param string $name Name of property.
     *
     * @return mixed|false Value of the property.
     */
    public function findAdditionalProperty(string $name)
    {
        if (isset($this->additionalProperties[$name])) {
            return $this->additionalProperties[$name];
        }
        return false;
    }

    /**
     * Encode this object to JSON
     *
     * @param bool $asArrayWhenEmpty Whether to serialize this model as an array whenever no fields
     *        are set. (default: false)
     *
     * @return array|stdClass
     */
    #[\ReturnTypeWillChange] // @phan-suppress-current-line PhanUndeclaredClassAttribute for (php < 8.1)
    public function jsonSerialize(bool $asArrayWhenEmpty = false)
    {
        $json = [];
        if (isset($this->tags)) {
            $json['tags']     = $this->tags;
        }
        if (isset($this->fallback)) {
            $json['fallback'] = $this->fallback;
        }
        $json = array_merge($json, $this->additionalProperties);

        return (!$asArrayWhenEmpty && empty($json)) ? new stdClass() : $json;
    }
}
